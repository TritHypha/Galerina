# RD-1295 Q1 / C17 Q2 follow-up — 2026-09-22

HEAD: `0f24ca30ef3f173c43a60c914c18b161327f2227` (uncommitted repairs).
Not production admission. Not complete RD-1295 closure. Independent audit pending.

## Q1 — resolver matches Node

Before: `pickEsmExport` iterated `["import","node","default"]`, so
`{node, import}` selected `import.js` while Node selected `node.js`.
`findInstalledPackage` searched `node_modules` before PACKAGE_SELF.

After: own-key insertion order against `{node-addons,node,import,default}`;
containing-package self-name before nested copies; require-only exports
refuse `unsupported-esm-export`.

Oracle: `import.meta.resolve` plus `import()` of the fixture entry.
Closed-set allow-lists that omit the runtime target fail.

Commands (package dir `packages-ts/galerina-tower-citizen`):

```text
npm.cmd run build
node --test tests/rd-1295-governance-isolation.test.mjs tests/rd-1295-products.test.mjs
```

Result: **75/75** pass, 0 fail, 0 skip. New controls:
`Q1 resolve: node-before-import…`, `import-before-node…`,
`package self-reference…`, `require-only exports refuse…`.

## Q2 — C17 own keys

Before: `properties[name]=` / `types[name]=` dropped `__proto__` while
`ok: true` and `required` still listed the name.

After: `defineOwn` stores every admitted field/record name. JSON
serialization keeps the keys. Docs OpenAPI refuses `__proto__` as a
reserved component/property (closed, not silent drop).
Option/Result/Decimal still refuse `FUNGI-CONTRACT-SCHEMA-003`.

Commands:

```text
npm.cmd run build   # galerina-core-compiler
node --test tests/contract-schema-export.test.mjs
npm.cmd run build   # galerina-docs after compiler dist
node --test tests/nested-contracts.test.mjs
```

Result: compiler export **6/6**; nested docs **12/12**.

## Profile map

Isolated `/tpl` `/photonic` `/custody` `/dataplane` are cluster exports.
Frozen `tower.tpl.v1` etc. are governance/kernel unions. air-gap, registry,
api-data, full-lab have no admission helper. Table:
`packages-ts/galerina-tower-citizen/PRODUCT-LINE.md`.

## Residual classes

| Item | Class | Locator |
|---|---|---|
| C16 durable replay | explicitly deferred feature | `replay-store.ts`; `RD-1286`; API TODO HOLD |
| C20 captures / word-boundaries / findAll | explicitly deferred feature | `RD-1292` refused profile |
| JSON fractions as Decimal | missing owner decision | `RD-1289`; JSON freeze refuses IEEE numbers |
| Option.zip | missing design | `type-checker.ts` + `stdlib.ts` anonymous `{first,second}` |
| core-network Tower package install | architectural HOLD | install ≠ runtime TCB |
| 7291 compiler suite | NOT VERIFIABLE here | not rerun this follow-up |

The 123-failure compiler report is historical and superseded.
