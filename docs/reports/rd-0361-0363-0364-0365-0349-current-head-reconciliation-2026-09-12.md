# RD-0361 / RD-0363 / RD-0364 / RD-0365 / RD-0349 current-head reconciliation

Date: 2026-09-12
Scope: Galerina repository only; read-only R&D reconciliation plus two bounded runtime corrections.

## Evidence boundary

The review began from Galerina baseline commit
`30c54db4e686197579569076219b1dd0ff9a6750`; the bounded implementation and
this report are committed together. The final commit and tree are reported in
the handoff that names this file (a commit cannot embed its own hash without a
self-reference). The checkout already contained
unrelated Myco working-tree changes; they were preserved. No full 2,720-file
corpus run, `.fungi` authoring wave, production activation, branch merge, or
remote push was performed.

GPT-6 Astra independently reviewed each record and the current source. The
newer private KB handovers were checked with `node scripts/kb-index.mjs`,
including the 2026-07-19 RD-0363/0364/0365 wiring directions and acceptance
checklist, the RD-0361 full-sync history, and the 2026-07-17/18 RD-0349
registry adjudications. Older receipts were treated as historical when their
head or implementation scope did not match the current checkout.

## Focused evidence

- `node scripts/audit-kernel-fungi-twins.mjs` — **104/104 check-clean** across
  ten directories: 74 shadow, 1 differential, and 29 authoritative.
- Focused RD-0363/0364/0365 compiler, runtime, and Tower tests — **21/21**.
- Fresh compiler build (`packages-ts/galerina-core-compiler`, `npm run build`)
  — passed after the new focused test was admitted to the build input set.
- RD-0349 registry, exact-scaling, runtime-registry, and new I2 tests — **25/25**.

The focused tests used the freshly rebuilt compiler `dist`; they do not claim a
fresh full-estate build or production admission.

## Dispositions

### RD-0361 — execution cutover ladder

**Bounded audit PASS; R&D track PARTIAL/OPEN.** The current audit confirms the
104 source/twin checks and the exact execution-column split above. Astra found
that this is lexical/source-test and `galerina check` evidence. It is not a
fresh differential execution bake, deletion decision, SLIDE/VOK admission, or
owner authority flip. Those remain separately required and are not inferred
from the count.

### RD-0363 — passive-plan replay admission

**Focused controls PASS; R&D track PARTIAL/OPEN.** Hash, freshness, target,
unsigned-plan refusal, and present-but-unverified signature refusal tests pass.
`verifyPlanAdmission` now keeps a signature at INDETERMINATE until a downstream
Ed25519/ML-DSA verifier supplies proof; optional admission fields are not all
bound into the canonical hash; `executePlan` is not itself an admission gate;
and unsupported step kinds are not a terminal refusal. The signer/key policy,
authenticated envelope, replay-time capability recheck, and receipt binding
need an owner-approved runtime seam.

### RD-0364 — governed inference bridge

**Focused controls PASS; R&D track PARTIAL/OPEN.** Canonical effects, model
allow-listing, call/token caps, and the Fungi governance surface are present.
The hybrid engine still runs its deterministic Stage-A demonstration vector;
real model weights/provider identity, verified-to-authorized transition,
egress/redaction proof, complete spend enforcement, and receipt binding remain
open.

### RD-0365 — TPM / secure-enclave key custody

**Focused controls PASS; R&D track PARTIAL/OPEN.** The host profile seam and
strict custody ordering are tested. `keyCustody` is still informational
metadata; `UNKNOWN_HOST` and the named profiles do not prove a vault, TPM,
hardware signer, or attestation. External vault evidence, host resolution in
the authorization consumer, and L2–L4 attestation remain open.

### RD-0349 — value-unit registry and Money precision

**I2 runtime precision CLOSED by this change; broader RD PARTIAL/OPEN.** The
generated `MONEY_MINOR_UNITS` registry already supplies the scales, but
`moneyDecimals` returned `2` for every currency. It now reads the registry,
accepts a valid zero scale, and refuses a Money-marked value whose currency has
no admitted scale; Money/Money ratios retain their separate 18-place scale.
The focused test is
`packages-ts/galerina-core-compiler/tests/stdlib/rd-0349-money-scale-runtime.test.mjs`.

Commodity/Crypto/Rate/Percent coverage, custom-unit policy, and overflow and
rounding policy beyond this I2 correction remain outside this bounded change.

## Next owner-gated work

1. Define and admit the authenticated plan-signing/verification seam for
   RD-0363, then bind execution to it.
2. Supply real provider/weight, egress, budget, and receipt evidence for
   RD-0364.
3. Supply evidence-backed custody and attestation for RD-0365.
4. Decide the RD-0361 authority/deletion/SLIDE/VOK gates.
5. Continue the remaining RD-0349 unit families only after their sourced
   scales and policies are explicit.

None of these dispositions authorizes bulk `.fungi` translation or changes the
retained TypeScript/native boundaries.
