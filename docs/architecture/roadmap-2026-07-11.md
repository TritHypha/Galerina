# Galerina roadmap — 2026-07-11 (current-state, post type-checker burn-down)

**Snapshot:** branch `refactor/spore-format` · suite **92/92 · 7,014 · 0 fail** · all gates GREEN except the known **#20 diagnostic-codes baseline (165, owner-gated)**. Component health **92/94 (97.9%)**, 13 families 100%, 2 exempt orphans.

This is the living roadmap; the owner pushes. Honesty line (binding): **"100% + no `.ts`" is two things** — (a) the governed **decision surface** in checker-verified `.fungi` (achievable now, being done), and (b) **deleting the `.ts`** (execution-switch-gated: #143 WASM byte-parity + the DSS.wasm TCB, partly hardware-gated). The `.ts` stays the executed artifact until (b) lands.

## 1. Stage-6 self-hosting (`.ts → .fungi`) — the owner's headline arc

**Owner sequence (in order, each toward "100% + no `.ts`"):** ① Type checker → ② WAT emitter → ③ Runtime interpreter → ④ Passive Execution Plans & Target Bridges.

| Surface | State | Next |
|---|---|---|
| **Effect checker** `.fungi` | **6/6 codes — diagnostic parity COMPLETE** | rides the execution switch |
| **Type checker** `.fungi` twin | **10 codes** `{001,002,004,005,006,007,008,019,NAME-002,TYPE-020}` — up from 6 this session (008 parity + a real symbol-table/scope/param-seed capability) | **parser-shape tranche** (below) — the "current-shape" vein is exhausted |
| **Kernel** governed surfaces | **4/4 decision surfaces** twinned (`secret-gate`, `route-defaults`, `registry-index`, gate-6 auth) + gated | behavioral parity → execution switch |
| **Tower-citizen** governance core | `quorum` + `lease` K3 folds twinned | more decision surfaces (data-plane-border, ai-governance) |
| **GIR emitter** | already `.fungi` | — |
| **② WAT emitter / ③ interpreter / ④ bridges** | NOT started; large + partly floored (wasm-instantiate/FFI/crypto) — same bounded-TCB situation as compiler/kernel | twin the decision surfaces; host floor → declared shim |

**Type-checker parser-shape tranche (the next phase — each = `parser.fungi` emits a new field → `type-checker.fungi` checks it → differential tests):** `003` brands · `009/010` generics · `011` collection-element · `012` Result · `021/022` match-arm exhaustiveness (highest value — start here) · `023/024` Auto · `016/017/030` tensors.

**The gate on literal "no `.ts`":** the execution switch — Stage-B WASM byte-parity (#143, only `tokenize` today) so `.fungi` compiles to a TS-consumable library; the kernel/compiler are TS libraries imported by ~10 packages, and the `.fungi` build emits `.wasm`, not a `.d.ts`. This is the one program that finally deletes the `.ts`. Assurance layer (#29: fuzz differential-oracle + scoped Z3) gates each stage flip.

**Upstream-of-compiler `.fungi` scope (checked 2026-07-11).** The compiler's 4 internal upstream deps split by nature: **substrate-math** (pure math), **devtools-graph-algorithms** (bfs/dfs/reach/topo/dominators + graph structures — pure algorithms), and **core-security** (crypto primitives) are the **bounded TCB / host+compute floor** — they hold NO governed decision surface, so they are correctly `.ts`/native and are NOT `.fungi`-twin candidates ("dense compute stays in `.fungi fu` bodies; crypto is the frozen floor"). Only **core-network** (inbound/egress guards, cors-policy, cert-gate, defensive-controls, admission-feedback) carries genuine network-**border governance** — a real future twin candidate, but net-new scope already tracked as #24 (rate-limit) + #41 (defensive-controls, partly unwired) rather than a regression. **Conclusion: no urgent upstream conversion; the Stage-6 twin focus stays the compiler's own pipeline (② WAT → ③ interpreter → ④ bridges) and its decision surfaces.**

## 2. Security / API queue (RD-0320 arc)

- **DONE this arc:** #10 gate-6 mTLS presence bypass · #23 DNS-rebind SSRF · #25 SECURITY contact · #26 supply-chain (Dockerfile/SBOM) · #27 JWT + deny-CORS · **#22 WAT loop fuel cap (live WASM DoS)** · **#41 defensive controls** (verified-trusted-proxy, uniform responses, bounded pagination, opaque IDs + 2 test classes).
- **Open (unblocked):** #24 rate-limit cluster wiring (needs atomic check-then-act, RD-0333 F2) · wire #41 controls into `route-defaults`/kernel + the built-but-unwired `posture.ts`.
- **RD-0333 F3 verified:** the kernel releases the concurrency slot on all paths incl. handler-throw (pinned regression).

## 3. Owner-gated (decisions that unblock the most)

1. **#20 diagnostic-code taxonomy** — the decision-ready proposal resolves every conflict (008/002/023 splits, UPPER_SNAKE names); it unblocks the FULL type-checker 22/22 and clears the last red audit. Fixtures are code-keyed → the renumber is mechanical + safe.
2. **D2 audit-key rotation build** — design complete (phased triple-lock, append-only, destroy-vs-revoke-then-archive); answer §6 (quorum M, cadence, placement, canary N, retire policy) → I build steps 1–4 (pure, no live keys).
3. **RD-0341** — a proceeding `_ =>` in a governance match is a latent open-sum fail-open (verified via the graph); fix is a breaking-risk policy change (recommend warn-first).
4. **RD-0340 field↔cut rung-3 hoist — LANDED 2026-07-11** (ZT `4ec48a5`, Galerina `9d67e087`). The `[name:cut(<field>)]` annotation is parsed, version-gated to `@version 1.2.0` (new ladder 1.0.0<1.1.0<1.2.0; prod stays {1.0.0}, fail-closed), and bound to the PRIVACY rule (rung-3): a WRONG field REJECTS, an un-annotated privacy cut WARNS (Option-2 on-ramp). SPEC v0.5→v0.6 (§1.8); self-test 164→172; keep-green 15/15; examples 01–05 migrated. **Owner-gated residual:** the **Option-1 breaking flip** — REJECT (not warn) an un-annotated privacy cut, forcing every privacy `.gate` to `@version 1.2.0`. Recommend scheduling the flip once the tree is annotated.

## 4. Performance posture (benchmark snapshot 2026-07-11)

Full run in `packages-galerina/galerina-devtools-benchmarks/results/latest.json`. **Honest read:** the **governed** path (`galerinaGoverned`/`galerinaManifest`) pays a large but bounded governance tax vs native (e.g. compute-mix: governed ~29 ops/s vs node 133M) — that is the safety-over-speed trade the whole architecture exists for. The **passive** `.fungi` path (`galerinaPassive`, no governance) is competitive-to-leading on several workloads (fibonacci, matrix-multiply, gpu-compute, nbody, json-parse) but those "wins" partly reflect differing per-runtime workloads (unit-alignment flags `tri-logic`/`data-query` as incomparable). Native Rust/Node lead raw compute (arithmetic, tower-of-hanoi, mandelbrot, spectral-norm). No "fastest" claim — the value is *governed* execution; the passive path shows the ceiling once the governance layer is optimized/compiled.

## 5. Near-term (autonomous, in priority order)

1. Type-checker parser-shape tranche — `021/022` match-arms first (highest value; ties to the RD-0341 fail-closed-default question).
2. Wire #41 defensive controls into `route-defaults`/kernel (fold, per the RD-0325/0326 prompt).
3. More tower-citizen governance twins (data-plane-border IDOR fold, ai-governance No-Coercion).
4. On owner decision: #20 burn-down (unblocks 22/22) · D2 build · RD-0341 warn.
