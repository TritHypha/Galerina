# Syntax Update — Live Tracker

**Owner decisions LOCKED 2026-07-08 (mid-session Q&A):**
1. **Keywords: HARD-RESERVE + codemod** — new construct keywords AND the rename aliases become real reserved keywords; the codemod renames the ~19 colliding identifiers (check 6f/13× · project 4f · any 1f · authorize 1f; aliases have zero corpus collisions).
2. **`destination` only in the NEW schema construct** — existing contract `intent {}` blocks keep their name/meaning (prose intent ≠ routing); no corpus churn.
3. **A18 tenant scope stays sequenced AFTER W5–W7** (rides the new schema/taint surface), then its own focused package.
4. **Commit at milestones** — LOCAL commits with explicit pathspecs at each green gate; push stays HELD for explicit owner GO.

**R&D-session decisions LOCKED 2026-07-08 (KB `f233723`), implemented here:**
- **Q1: `@version 1.0.0` REPLACES `#gate`** — gate-parser reads+gates it (closed {1.0.0}; retired `#gate` rejected with a migration pointer); 5 corpus `.gate` migrated via codemod `--gate-stamp`; RULES.md R1 + AI-AUTHORING-GUIDE updated; 14/14 gate tests. Corpus now **413/414 headers valid** (the 1 absent = the ceremony-frozen greeting source, by design).
- **Q2: `UNKNOWN` is the canonical K3 middle state** (DENY(−1) < UNKNOWN(0) < ALLOW(+1)); HOLD/INDETERMINATE = eradicate-aliases; `ambig:` keyword unchanged. W5a implements on this naming.
- **Q3 (was open) — RECONCILED, maths checked:** 409 `.fungi` = 407 in the stamps commit + example-465 in the compiler commit + api-protocol-rest… all 408 stampable stamped + 1 ceremony-frozen (greeting) = 409 ✓; +5 `.gate` = 413/414 present ✓. The R&D session's "275" was a mid-migration observation.

**2026-07-08 continued (anti-hallucination + verification sweep + owner Q&A):**
- **Anti-hallucination for `.fungi` (owner request) — ☑ DELIVERED.** The `.fungi` compiler is ALREADY a numbered multi-phase pipeline; every "unknown ⇒ reject" gate is an anti-hallucination gate. Made it EMPIRICAL: `tests/anti-hallucination-corpus.test.mjs` (26/26) feeds 11 hallucination classes + 2 valid controls through the REAL phases (parse/effects/types/governance), asserting 0% pass-through, per-entry designated-phase wiring, defense-in-depth (H6 caught by ≥2 independent phases — the "don't trust one check" made concrete), and anti-vacuous controls. Design: `docs/ANTI_HALLUCINATION.md`.
- **did-you-mean (self-correction + R&D version-format steer) — ☑ effect names.** `FUNGI-EFFECT-004` now nudges a likely TYPO to the nearest canonical name (Levenshtein ≤ max(2,len/4)); wild inventions get no misleading nudge; fail-closed unchanged. `tests/effect-did-you-mean.test.mjs`. REMAINING: capability/type/sink names + version-format did-you-mean.
- **Owner Q&A LOCKED (via R&D session):** (1) **version format = KEEP DUAL** — `.fungi @version 1` = grammar version; `.gate @version 1.0.0` = signed-artifact compat floor (A4); mitigate confusion with did-you-mean+lint, NOT by collapsing. (2) **aliases = ADDITIVE** with 4 pins: desugar-identity lint (one IR per spelling) · docs teach one canonical spelling + alias table · lint counts corpus spelling usage (data-driven) · `return`/`project`/`release`/`deflect` is 1→3, never let a codemod pick one. → both fold into W5b/W6.
- **Verification sweep (owner: "re-run all dev tools graphs, audits and tests") — ☑ GREEN.** Tests **61/61 · 6161**; proofs **56/57** (1 red = rd-0128 known-aspirational). Fixed 2 REAL red proofs (W4 disk-fixtures lacked `@version` — stamped, `a4cc9778`). Audits: signed-fixture-drift ☑ (api-protocol-rest BOUNDARY.md was graph-all line-ending churn — reverted, empty diff), package-border ☑ (fungi-scan baseline established, `ac9cc4e3`), syntax/doc-drift/checker-wiring/effect-canonicality/corpus-effect-names/name-collisions all ☑. Standing backlogs (NOT this work, pre-existing): diagnostic-codes 171 · coverage 8 · perf 6 · provenance 3.
- **⚠ FOUND (owner action): uncommitted `.tmf`→`.spore` rename** across 99 tree files (mtime 15:19, HEAD still says `.tmf`) — NOT this session's work, NOT in any of my commits. Preserved untouched (did not revert, did not conflate into my commits). Owner to reconcile/commit. Also: `build/*` dev-tool regeneration left uncommitted (regenerable).
- **git:** owner granted commit+push authority; pushed 15 green commits `3b9340e1..97fdebc6`, then `4a928e6b` (did-you-mean). Origin: TritHypha/Galerina main. Later pushes: branding `7e14da23`, diag-naming `13d60658`, RD-0277 bench adopt `1a422680`, FUNGI-TYPE-024 `05bb1a4d`, path-fixups `88f79272`, TODO `f1ac8e85` (+ KB `a505c58`/`31abee2`/`143476e`).
- **W5b codemod PRE-WORK (check-don't-assume, owner: "run codemod now") — collision surface DE-RISKED, hard-reserve DEFERRED-to-bundle:** the feared "~19 colliding identifiers" is really tiny. **Corpus (.fungi): 2 renames only** — binary-trees `check` var→`acc`, pattern-10 `authorize` flow→`authorizeVerdict`. **`project` is NOT reserved** (owner pin: `project "Name"` is a distinct top-level declaration ⇒ a word with distinct semantics is a construct, not a return-alias; `release`/`deflect` are the clean return-aliases, 0 collisions). **`any` already handled (W5a).** **Method-names survive** — `obj.check()` parses fine when `check` is a keyword (verified: `obj.match()`/`obj.for()` already work). **BUT hard-reserving `check`/`fault`/`authorize` broke 6 TEST FIXTURES** (`flow check(...)` in `.test.mjs` strings — interpreter/parser/flags-and-manifest/trap-decl/compiler-safety/domain-boolean) entangled with test logic (`executeFlow("check")`, manifest name asserts). **fungi-scan GAP:** it scans on-disk `.fungi` only, NOT inline `.fungi` fixtures in `.test.mjs/.cjs` — same class as the earlier P5/N3 proof-fixture miss. **DECISION:** reserving `check`/`fault` as DEAD keywords (before their `check{}`/`fault` construct exists) breaks entangled fixtures for zero functional gain — so the hard-reserve BUNDLES with T2.2 construct semantics (rename corpus + fixtures + test-logic in one complete green increment). Reverted the premature reserve to green (HEAD `f1ac8e85`, 6168 tests). See [[galerina-fungi-scan-inline-fixture-gap]].
- **W5b T2.2 `check{}` construct — ☑ MVP LANDED (`fa830df2`).** `check(subject){ if:/deny:/ambig: }` K3 tri-branch: parser (checkExpr/checkArm; `deny`/`ambig` contextual arm labels, no reservation), interpreter fail-closed dispatch (DENY(-1)=deny/UNKNOWN(0)=ambig/ALLOW(+1)=if; non-verdict subject or missing arm TRAPS → audit.result=error), **FUNGI-CHECK-001** exhaustiveness (all 3 arms, RD-0240), **FUNGI-CHECK-002** verdict-only subject (A9), and VERIFIED effect-safe (undeclared effect inside an arm still caught — checkers descend generically, no blind spot). Stage A (codemod + hard-reserve check/fault, 6 fixture renames done properly) `519e15e7`. Tests `check-construct.test.mjs` 11/11; full suite **62/62 · 6200**. **REMAINING T2.2:** WAT lowering for `check{}` (tree-walker only for now) + the `fault` audited channel (A10 — needs its surface-syntax spec before building; won't guess). Codes mirrored to KB (`0d22c11`).
- **Bug-fix pass (owner: "fix all bugs + security before moving on"):** `galerina-ext-bridge-quantum` was moved back into packages-galerina (untracked, move-broken imports `../../Galerina/packages-galerina/...` doubling the path → ERR_MODULE_NOT_FOUND); fixed to bare `@galerina/X` specifiers in src+tests, corrected `file:` deps, re-added the package to git + workspace/version registries. 21/21 quantum, full suite **62/62 · 6189**. Security audits all GREEN; remaining audit fails (coverage/diagnostic-codes 165/perf/provenance-stale) are pre-existing curation backlog, not fresh bugs. RD-0280 asks answered in `packages-galerina/galerina-core-compiler/TOOLING_API.md` (`c65bbe7e`).

**Plan:** [SYNTAX_UPDATE_PLAN.md](SYNTAX_UPDATE_PLAN.md) · **Anti-hallucination:** [ANTI_HALLUCINATION.md](ANTI_HALLUCINATION.md) · **Legend:** ☐ not started · ◐ in progress/partial · ☑ done+verified (evidence required) · ✖ blocked/deferred
**Updated:** 2026-07-08 (PM — anti-hallucination + verification sweep green)

| ID | Item | Status | Evidence / note |
|---|---|---|---|
| W1 | Plan + tracker + todo ledger | ☑ | docs exist; harness tasks #7–#13 registered |
| W2 | `galerina-devtools-fungi-scan` package (lexer-based corpus scanner) | ☑ | 17/17 tests; in workspace.json; `build/fungi-scan/` |
| W2.b | Baseline scan of 409 `.fungi` + 5 `.gate` (pre-change snapshot) | ☑ | **414 files · @version 0/414 · &&/\|\| 5 files (11×) · vAnd/vOr/vNot 0 · match-without-`_` 0/213 (!) · collisions: check 6f, project 4f, any 1f, authorize 1f · 2 pre-existing lex-error example files · strict=373 (=366 no-header +5 legacy +2 lex)** |
| T0.1b | `FUNGI-MATCH-001` warning→error (+neg-tests) | ☑ | **ERROR + STRUCTURAL** (name-heuristic + arm-count gate removed — they were themselves fail-opens); 6 neg-tests `tests/governance/match-exhaustiveness.test.mjs`; corpus fallout 0 (baseline predicted it) |
| A23 | drain→reject at governance-context `skipBalancedBraces` sites + lint | ☑ | **FUNGI-SYNTAX-011** reject-then-recover at 14 sites (contract/secrets×3/authority/policy/guard/access/gate/emergency/contract-set/import); **collect-don't-drop** inside KNOWN contract sub-blocks (`retries { network.outbound {…} }` etc. now VISIBLE in AST); **FUNGI-SYNTAX-013** rejects inert top-level `governance {}` (zero consumers). **Caught 2 real latent bugs:** example 465 had `policy {}` nested in contract (drained ⇒ never verified — moved to flow level like 462/463) + PCI fixtures used `target {}` (wrong name, s/b `targets`). Desync-recovery property preserved (updated test). Lint = W7 |
| A20 | authority refs resolve-or-deny | ☑ | **FUNGI-ACCESS-001** now resolves grants against ADMISSION_CAPABILITIES ∪ CANONICAL_EFFECTS (alias-aware, single-sourced); dotted-name bypass REMOVED (`totally.fake.capability` admitted silently before); error in production/deterministic, warning in dev (GOV-004 house pattern); 7 tests `tests/governance/access-grant-resolution.test.mjs`. `conforms_to`→GOV-004 + `parent_policy`→INHERIT-001 already fail closed. PROMPT §8 "checkTransition 0 callers" = STALE (wired in RD-0236: bitnet bridges + tpl-simulator) |
| A1 | classification: unknown ⇒ most-restrictive, never public | ➡ | moved into W5b — applies to the `schema` construct that lands there |
| T1.1 | `@version` header grammar (`.fungi`) | ☑ | `parseProgram` pre-lex gate: FUNGI-SYNTAX-014 (malformed/below-floor/above-current) + 015 (absent on disk paths); header BLANKED not stripped (line numbers exact); BOM-tolerant; `FUNGI_MIN_SUPPORTED_VERSION=1`/`FUNGI_CURRENT_VERSION=1` exported; 9 tests `tests/version-header.test.mjs`; codes registered in KB `compiler-diagnostics.md` |
| T1.1b | corpus stamped | ☑ | codemod `--stamp --apply`: **409/409 .fungi** (incl. api-protocol-rest — its manifest is UNTRACKED dev output, so stampable); `greeting` = the one genuinely ceremony-frozen pkg (git-tracked signed manifest) — frozen, ceremony-owed |
| T1.2 | reject unknown+absent+below-floor at read paths | ◐ | DONE: `.fungi` parser (both CLIs — 5 cli.ts + 3 galerina.mjs sites `requireVersionHeader:true`; git-diff sites exempt) · GIR reader **absent now rejected too** (`wat-emitter` — the "internal partial-GIR" tolerance was an unauthenticated bypass) · fuse-loader (closed set {fungi.fuse.v1, **lln.fuse.v1**=pre-rename alias for the signed greeting fixture, ceremony-owed}). DONE ALSO: **`.gate` `@version 1.0.0` gate** (Q1) — gate-parser reads closed set {1.0.0}, retired `#gate` rejected w/ migration pointer; 14/14 tests. **VERIFY-LOOP COHERENCE (owner audit 2026-07-08):** the anti-hallucination guarantee rests on the verify-loop (author runs `gate-check.mjs`, green=valid) — Q1 had split-brained it (docs+compiler said @version, the CHECKER still required #gate). FIXED across all THREE surfaces: reference checker `ZT-Galerina/tools/gate-check.mjs` (@version closed set + retired-#gate reject; **self-test 135/135**; 5 corpus files **5/5**), `SPEC-gate-language.md` (EBNF+evolution), and the Galerina `docs/language/gate/*` 8-file reference (`0d8f1752`). REMAINING: `.lmanifest` CBOR reader version check (legacy lln.manifest.v1 ceremony-coupled) |
| A4 | version header inside signed region + floor | ◐ | floor ✓ (SYNTAX-014 below-floor reject); signed-region: `.fungi` header is part of source ⇒ covered by sourceHash — assert in W7 conformance |
| — | **Learned-class hardening (owner rule: encode checks into tools)** | ☑ | codemod: **signed-frozen guard** (skips signed+git-tracked-manifest pkgs; caught by `audit-signed-fixture-drift` when my stamp dirtied 2 signed pkgs) · fungi-scan: `signed-frozen` corpus class (strict-exempt, loudly counted) + test · drift auditor now prints **old-brand schema spellings per signed pkg** (ceremony scope visible: greeting = lln.manifest.v1 + lln.fuse.v1) · test-authored fixtures stamped at their writers (5 test files) · **fungi-scan inline-fixture scan** (`inline-fixtures.ts`, 2026-07-09): stateful non-regex host scanner extracts backtick `.fungi` from `.mjs/.cjs` → same real-lexer path; **1840 fixtures / 194 hosts** now visible, surfacing `check(10f)`/`all(6f)`/`authorize(4f)`/`project(4f)` collisions the disk scan missed (the W4 @version-proof + W5b keyword-reserve blind spot); always test-corpus/strict-exempt; 25/25; W6 codemod can reuse `extractFungiFixtures` |
| T2.1 | `flip` + `all{}`/`any{}` + Verdict/Bool overload rules (A9/A12) + truth-table tests | ☑ | **DONE `02252fc7`** — 15/15 `tests/k3-operators.test.mjs` (every cell both tiers: walker + signed/admitted WASM; De Morgan ×9; A9 gates FUNGI-K3-001/002/003 registered; Bool-lane regression guard); compiler 4,304/4,304; repo 61/61 · 6,131. A12 lint → W5b (needs authorize positions). Original map: **Implementation map (locked):** new primitive type `Verdict` (WAT i32; values −1/0/+1 per Q2 lattice); producers = `Verdict.Deny/Unknown/Allow` member-constants (enum-tag-style lowering); `and`/`or` stay parse-desugared to `&&`/`\|\|` — the TYPE-CHECKER overloads on operand types (Verdict×Verdict ⇒ min/max; Bool×Bool ⇒ boolean; MIXED ⇒ FUNGI-K3-001 error, A9); `flip(x)` unaryExpr, Verdict-only (= i32 negation — flip(−1)=+1, flip(0)=0 ✓ maths), `!` on Verdict ⇒ FUNGI-K3-002; `all{…}`/`any{…}` = k3FoldExpr min/max folds, children must ALL be Verdict (else FUNGI-K3-003); **empty all{} ⇒ UNKNOWN(0)** (spec anti-vacuous override of min's ALLOW identity), **empty any{} ⇒ DENY(−1)** (max identity + zero-trust); WAT: min/max via `select`, fail-CLOSED throw when the emitter cannot prove Verdict operands (never guess-lower); interpreter: `verdict` dispatch tag. Proof: `proofs/k3-truth-tables-proof.mjs` — all 9 and/or cells vs min/max, 3 flip cells vs negation, De Morgan ×9, fold identities, lattice order. A12 lint lands with W5b's authorize positions. `and`/`or` keywords already active (`lexer.ts:175`) |
| T2.2 | `check` + `fault` audited channel (A10) | ◐ | **check{} DONE `881d354f`** — `check(v){ if:/deny:/ambig: }` K3 tri-branch: parse→checkExpr/checkArm; interpreter fail-closed dispatch (DENY→deny/UNKNOWN→ambig/ALLOW→if); FUNGI-CHECK-001 exhaustive (all 3 arms required); FUNGI-CHECK-002 verdict-only subject (A9); effect-safe (checkers descend into arm bodies); WASM fail-closed `unreachable` trap (guarded by test, full lowering deferred); 12/12 `check-construct.test.mjs`; `check`/`fault` hard-reserved (lexer V1_ACTIVE). **fault = raise-only MVP** (owner LOCK 2026-07-09): `fault <expr>` ALWAYS halt+audit+deny, unswallowable, no handler syntax yet — NEXT |
| T2.3 | `sealed auto schema` + inject pass (A6/A7) + `through S` schema-lock + §6 defaults | ☐ | owner LOCK 2026-07-09: **build ZT MVP now** (I design the surface — deny-by-default, authorize+intent required, unknown-field ⇒ most-restrictive per A1) |
| T2.4 | deny-only `prefilter` + dominator check (A8) | ☐ | owner LOCK 2026-07-09: **build ZT MVP now** (I design the surface — deny/maybe only, no allow branch; dominator = no governed sink on a maybe-path before authorize) |
| T2.5 | `unsecure`/`secure flow`/`purify` (A2/A11/A17) | ☐ | |
| T2.6 | lexer alias table + desugar-identity lint | ☐ | |
| T3.1 | `scripts/migrate-fungi.mjs` codemod | ☐ | |
| T3.1b | H2-a taint-default flip (lands WITH codemod — owner LOCK) | ☐ | |
| T3.2 | migrate all `.fungi` (409) + `.gate` (5) + `@version` | ☐ | |
| T3.3 | regenerate GIR/manifests · rebuild kernel+GIR+compiler · local re-sign | ☐ | |
| T4.1 | detectors wired into lint-conventions/run-phase-close | ☐ | |
| T4.2 | `.gate`→GIR→WASM→run e2e conformance (pipeline test, non-runtime) | ☐ | |
| T4.3 | verify-by-running deny suite (forged/unclassified/raw/old-version/ungoverned — anti-vacuous A27) | ☐ | |
| T4.4 | full recursive suite + component-health + audits green | ☐ | baseline today: 61/61 · 6,086 |
| — | **Verified already landed (2026-07-04 set, re-checked in code this session)** | ☑ | RD-0240-WAT · BK-1 · BK-2 · BK-3(downgraded) · BK-5/H1/M1 · H3-safelist — see plan §1 |
| ✖ | A18 tenant scope (BETA BLOCKER — next work package) · A3/A16 `.hypha` · A13/A14/A15/A24/A25/A26/A19 · RD-0238 P0 · C2 upgrade | ✖ | tracked in plan §2 "Deferred" — not dropped |
