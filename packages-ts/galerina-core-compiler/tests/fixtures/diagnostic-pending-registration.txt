# Diagnostic codes emitted by the compiler but not yet in a registry doc.
# Policy: SHRINK-ONLY. Register a code in compiler-diagnostics.md (with a description) and
# remove it here. The conformance test (diagnostic-namespace.test.mjs) fails on any NEW
# unregistered code, and on any entry here that is already registered.
#
# 2026-06-16: baseline was 65; ALL reconciled into compiler-diagnostics.md. Allowlist now EMPTY.
# Every emitted FUNGI-* code is documented. Keep it that way — register new codes, don't grow this list.
#
# 2026-07-24: FUNGI-ADMIT-001/002/003 (flagship param admission, COMMIT 1) briefly sat here pending KB
# registration; R&D registered them in compiler-diagnostics.md (b21c59c) — shrunk back to EMPTY.
#
# 2026-07-24: FUNGI-TYPE-033 (S1 condition-type gate — if/while must be Bool, bridge 0148) briefly sat here
# pending KB registration; R&D registered it in compiler-diagnostics.md (line ~1319, "registered 2026-07-24")
# — shrunk back to EMPTY. The shrink-only gate (diagnostic-namespace.test.mjs) drove this removal.
#
# 2026-07-24: FUNGI-K3-004 (Verdict compared to a non-Verdict, `v == b`) + FUNGI-K3-005 (non-Verdict returned
# as Verdict, `return 2`) — the two A9 generic-code fail-opens (minted `334689ad` per R&D GO bridge 0174)
# briefly sat here pending KB registration; R&D registered both in compiler-diagnostics.md FUNGI-K3 block
# (`3bae114`, source-verified at the mint sites, bridge 0177) — shrunk back to EMPTY. The shrink-only gate
# (diagnostic-namespace.test.mjs) drove this removal.
#
# 2026-07-24: FUNGI-VALUESTATE-011 DECLASSIFIER_NAME_SHADOWED (fail-closed floor against redact/seal/encrypt
# name-shadowing, CWE-501; minted per R&D GO bridge 0200, main `c365fc8d`) briefly sat here pending KB
# registration; R&D registered it in compiler-diagnostics.md (`aaf5ab0`, line 1234, source-verified at the mint
# site value-state-checker.ts scanDeclassifierShadows, bridge 0207) — shrunk back to EMPTY. The shrink-only gate
# (diagnostic-namespace.test.mjs) drove this removal.
#
# 2026-09-21: C14/C17/C20 codes minted in-tree pending KB registration.
FUNGI-BLOCK-005
FUNGI-CONTRACT-SCHEMA-001
FUNGI-CONTRACT-SCHEMA-002
FUNGI-CONTRACT-SCHEMA-003
FUNGI-CONTRACT-SCHEMA-004
FUNGI-CONTRACT-SCHEMA-005
FUNGI-PATTERN-001
