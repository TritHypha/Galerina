only after everthng else is done

do housekeeping, resolve outstanding bugs and security issues

do a full cycle though Zero-Trust thesis,  Build progress and Tracking registry update handover doc 

and then go full auto and go though each todo, and note why you cant finish it in the handover doc

run all graphs

run all audits

run all tests

fix anything that is found
do a full cycle though Zero-Trust thesis,  Build progress and Tracking registry update handover doc 

do housekeeping, resolve outstanding bugs and security issues

regenerate rebuild dist, regenerate package builds and regenerate the build folder

run full benchmark with full results in a UI chart and note anything unexpected

remember to make a chart where WASM is 0 anything faster is + anything slower is -
add tramlines for each test

do a % audit

update todo's

update roadmap

check handover doc for this round, update and present link
I/O — OS kernel
Packages
Memory
TLSTP — zero-middleware
WAT emitter
Runtime interpreter
App-framework layer
Post-Quantum & HW security
Passive exec plans & bridges
AI Inference Tower
work through these one by one now and try to get each to 100%
--------------------------

i will give you a loop, set it up a a cron not a routean

/loop 50 minutes. Continue full auto. Zero-trust every ambiguous fork — fail-closed,
least-privilege, verify don't trust.

1. Poll BOTH inboxes every tick: the session bridge to-main AND the KB coordination
   to-main. Handle every unprocessed note oldest-first — answering the newest is not
   "inbox clear". Read corrections FIRST: R&D may have retracted something you are
   already building on.
2. Resume from the work-state resume point, not your memory of it. git log -1 before
   any commit — R&D shares these checkouts.
3. Take ONE component to 100%, then move on — don't spread. 100% = its gates are green
   AND every number you publish is evidenced. A hand-typed percentage is not 100%.
   It is beta: "100%" never means shipped, released, or frozen.
4. Measure the effect of every change before keeping it. If you cannot verify it,
   revert it clean — as you did with the dangling-call decline. A change that "should"
   work is not a result.
5. Before vendoring anything from R&D, run its self-test AND check its surface against
   your tree. A prototype is a proposal, not a verified component.
6. BLOCKED ≠ STOPPED. Post the specific ask to to-rnd, park that item, pick up the next
   one. Only stop the loop when everything left is blocked or owner-gated — then say
   plainly what is blocked and on whom.
7. Commit, never push (owner pushes). Never skip hooks. Leave the tree clean at the end
   of every tick — no half-applied change.
8. Lead every report with what you retracted, reverted, or got wrong.

Take the time to get it right; a wrong answer delivered fast costs more than a slow
correct one.



/loop 50 minutes. Refresh the graph, run the tests, run the audits — then report the DELTA,
not the totals.

1. Graph: re-index / detect changes so lookups reflect HEAD. A stale graph is worse
   than no graph — it answers confidently and wrongly.
2. Report THREE separate counts: PASSED, FAILED, and DID-NOT-RUN. A gate that skipped
   is not a gate that passed. If a gate could not run (missing build artifact, absent
   input), that is RED, not clean.
3. Never turn a red gate green by disabling, skipping, or re-baselining it. Fix the
   cause. A baseline may only SHRINK — if you move one, say so and why.
4. Report the delta against the previous run: newly failed, newly passed, counts that
   moved. "All green" with nothing to compare against is not a report.
5. Anything newly red: diagnose to root cause before touching it, then fix the CLASS
   with a detector, not just the instance.
6. If nothing changed and nothing is red, say exactly that in one line and stop. Don't
   manufacture work.


 continue with WAT fix as priority 


And then P9 focus on the task at hand do not get distracted

 ----------------------------
/loop 20 minutes. Continue full auto. Zero-trust every ambiguous fork — fail-closed,
least-privilege, verify don't trust.

1. Poll BOTH inboxes every tick: the session bridge to-rnd AND the KB coordination
   to-rnd. Handle every unprocessed note oldest-first — answering the newest is not
   "inbox clear".
2. Resume from the work-state resume point, not from your memory of it. git log -1
   before any commit; two sessions share these checkouts.
3. Measure, don't infer. Run it, and read the REPORT a tool returns (diagnostics /
   warnings / status), not just the artifact. Code you have only read is a claim.
4. Verify any MECHANISM at source before telling main or me. A wrong observation
   wastes a measurement; a wrong mechanism misdirects someone else's work.
5. Don't hand-count — build the detector, self-test it, and check its SURFACE. A
   zero result is also what a broken probe returns: confirm "clean" / "not
   implicated" by an independent route before reporting it.
6. Label every claim: measured or inferred, live or latent — and say what you did
   NOT check. Establish liveness before raising an alarm.
7. Galerina is READ-ONLY. Commit and push only the KB.
8. Stop when both inboxes are clear and the rest is owner-gated: say so plainly and
   stop, don't invent work. Lead any report with corrections or retractions.

Take the time to get it right; a wrong answer delivered fast costs more than a slow
correct one.





when back on credits on WEDNESDAY...

R&D for P9 make sure there is an audit dev tool that audits all occurances of the food<> into a list so we can quickly deal with them, if we can cross compare to the ones already done even better

get R&D to tell you what it did for the gzip R&D using tri -1 then use GPT

priority is WAT and P9 for main

I have done some R&D into other areas check "<GitHub>\gpt\Security-infra designs"
and a seperate R&D in "<GitHub>\gpt\RD-0363-0364-0365 wiring"
and <GitHub>\gpt\RD-0363-0364-0365 wiring\README.md inc <GitHub>\gpt\RD-0363-0364-0365 wiring\src\rd0363-passive-plan-replay.ts <GitHub>\gpt\RD-0363-0364-0365 wiring\src\rd0364-governed-inference-contract.ts <GitHub>\gpt\RD-0363-0364-0365 wiring\src\rd0365-key-custody-ladder.ts <GitHub>\gpt\RD-0363-0364-0365 wiring\tests\rd0363-0364-0365.selftest.ts <GitHub>\gpt\RD-0363-0364-0365 wiring\docs\source-sighting.md <GitHub>\gpt\RD-0363-0364-0365 wiring\docs\acceptance-checklist.md


#owner phill
go through each rooamp to build and get R&D to make dev tools to analyse, find the code and help to complete the build such as searching for functions or code occurances



